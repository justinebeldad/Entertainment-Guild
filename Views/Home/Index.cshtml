@model List<Product>

@{
    ViewData["Title"] = "Home";
}

<h1>Featured Product</h1>

<p>Welcome to Entertainment Guild! We offer a variety of products with different genre on different medium.</p>


<h1>Product Categories</h1>

<div style="margin: 30px"></div>

<div><b>Books</b></div>
<a asp-controller="Product" asp-action="Add">Add New Book</a>
<table class = "table table-bordered table-responsive table-hover">
<tr>
    <th>Name</th>
    <th>Author</th>
    <th>Published</th>
    <th>Price</th>
</tr>
@foreach (Product product in Model)
    {
        if (product.Genre == 1)
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Author</td>
                <td>@product.Published</td>
                <td>n/a</td>
                <td>
                    <a asp-controller="Product" asp-action="Edit"
                       asp-route-id="@product.ID">Edit</a>
                    <a asp-controller="Product" asp-action="Delete"
                       asp-route-id="@product.ID">Delete</a>
                </td>
            </tr>
        }
    }
</table>

<div style="margin: 30px"></div>

<div><b>Movies</b></div>
<table class="table table-bordered table-responsive table-hover">
<tr>
    <th>Name</th>
    <th>Author</th>
    <th>Published</th>
    <th>Price</th>
</tr>
    @foreach (Product product in Model)
    {
        if (product.Genre == 2)
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Author</td>
                <td>@product.Published</td>
                <td>n/a</td>
                <td>
                    <a asp-controller="Product" asp-action="Edit"
                       asp-route-id="@product.ID">Edit</a>
                    <a asp-controller="Product" asp-action="Delete"
                       asp-route-id="@product.ID">Delete</a>
                </td>
            </tr>
        }
    }
</table>

<div style="margin: 30px"></div>

<div><b>Games</b></div>
<table class="table table-bordered table-responsive table-hover">
<tr>
    <th>Name</th>
    <th>Author</th>
    <th>Published</th>
    <th>Price</th>
</tr>
    @foreach (Product product in Model)
    {
        if (product.Genre == 3)
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Author</td>
                <td>@product.Published</td>
                <td>n/a</td>
                <td>
                    <a asp-controller="Product" asp-action="Edit"
                       asp-route-id="@product.ID">Edit</a>
                    <a asp-controller="Product" asp-action="Delete"
                       asp-route-id="@product.ID">Delete</a>
                </td>
            </tr>
        }
    }
</table>